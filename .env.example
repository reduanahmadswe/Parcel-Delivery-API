# ========================================
# PARCEL DELIVERY API - Environment Variables
# ========================================
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to version control

# ========================================
# APPLICATION SETTINGS
# ========================================
NODE_ENV=development
PORT=5000

# ========================================
# DATABASE
# ========================================
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/parcel-delivery?retryWrites=true&w=majority

# ========================================
# JWT AUTHENTICATION
# ========================================
# Legacy JWT secret (kept for backwards compatibility)
JWT_SECRET=your-super-secret-jwt-key-min-32-characters
JWT_EXPIRES_IN=7d

# Access Token (short-lived)
JWT_ACCESS_SECRET=your-access-token-secret-min-32-characters
JWT_ACCESS_EXPIRES=15m

# Refresh Token (long-lived)
JWT_REFRESH_SECRET=your-refresh-token-secret-min-32-characters
JWT_REFRESH_EXPIRES=7d

# ========================================
# SECURITY
# ========================================
BCRYPT_SALT_ROUNDS=12

# ========================================
# FRONTEND URL
# ========================================
# Used for CORS and email links
FRONTEND_URL=http://localhost:3000

# ========================================
# APP URL
# ========================================
# Used for tracking links and QR codes in emails/PDFs
# For development, use: http://localhost:3000
# For production, use your deployed frontend URL
APP_URL=http://localhost:3000

# ========================================
# EMAIL CONFIGURATION (Optional)
# ========================================
# Leave these empty if you don't want to enable email notifications
# For Gmail: Use App Password (not your regular password)
# Generate at: https://myaccount.google.com/apppasswords

EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password-here
EMAIL_FROM=notifications@yourdomain.com
EMAIL_FROM_NAME=Parcel Delivery System

# ========================================
# NOTES
# ========================================
# 1. For production, use strong random secrets for all JWT keys
#    Generate with: openssl rand -base64 32
# 
# 2. Gmail Setup:
#    - Enable 2FA on your Google account
#    - Create App Password at: https://myaccount.google.com/apppasswords
#    - Use the 16-character app password as EMAIL_PASSWORD
#
# 3. EMAIL_SECURE should be 'true' for port 465, 'false' for port 587
#
# 4. For production deployment (Render, etc.):
#    - Set NODE_ENV=production
#    - Use production MongoDB Atlas connection string
#    - Update FRONTEND_URL to your deployed frontend URL
#    - Ensure all secrets are different from development
